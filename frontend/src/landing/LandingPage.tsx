// Placeholder minimal — sera remplacé par la vraie landing en L3.
export function LandingPage() {
  return (
    <div style={{ padding: 48, color: 'var(--text)', fontFamily: 'var(--font)' }}>
      <h1>CleanVex (MonEspace)</h1>
      <p>La landing arrive bientôt. <a href="/app">Ouvrir l'app →</a></p>
    </div>
  )
}
